# Machine-readable phase registry — mirrors 05-features/INDEX.yml.
# One phase is active at a time. `active` names it (ACTIVE.md is the human-readable pointer).
# status: not_started | in_progress | locked
active: null
phases:
  phase-01:
    title: "Phase 01 — Foundation (example)"
    status: locked
    ac_total: 10
    ac_green: 10
# - one block per phase; /create-phase appends, /phase-finish sets status: locked -
